<img class="img-fluid img-thumbnail" src={% if post.picture %}
                                                                "{{ post.picture.url }}"
                                                            {% else %}
                                                                "{% static 'img/empty_cover.png' %}"
                                                            {% endif %} />
                                                            
                                                            
{% if page is not None %}
            <p>{{ page }} страница из {{ number_of_pages }}</p>
            <form method="get">
                {% if page > 1 %}
                    <a href="{{ request.path }}?page={{ page|add:'-1' }}{{ search_url }}">Предыдущая</a>
                {% endif %}
                <input name="page" type="text" />
                
                {% for search, value in search_dict.items %}
                    <input type='hidden' name="{{ search }}" value="{{ value }}" />
                {% endfor %}
                
                <input type="submit" value="Перейти"/>
                {% if page < number_of_pages %}
                    <a href="{{ request.path }}?page={{ page|add:'1' }}{{ search_url }}">Следующая</a>
                {% endif %}
            </form>
        {% endif %}
    ###################3    
{% extends "base.html" %}
{% load static %}

{% block title %}Лента{% endblock %} 

{% block content %}    
    <div class="row">
        {% for post in posts_list %}
            <div class="col-12 col-sm-6 col-md-4 col-lg-3 my-2">
                <a type="button" href="{% url 'post_detail' post.id %}" class="post-block text-center">
                    <img class="img-fluid img-thumbnail" src={% if post.picture %}
                                                                    "{{ post.picture.url }}"
                                                                {% else %}
                                                                    "{% static 'img/empty_cover.png' %}"
                                                                {% endif %} />
                    <h4 class="post-header">
                        {{  post.title }}
                    </h4>
                    <h5 class="post-author text-muted">{{  post.created_by.username }}</h5>
                    <div class="text-muted">{{  post.created_at }}</div>
                    <span class="oi oi-heart mx-2" title="Likes" aria-hidden="true"></span>{{  post.likes.count }}
                    <span class="oi oi-comment-square ml-4 mr-2" title="Comments"></span>{{  post.comments.count }}
                </a>
            </div>
        {% endfor %}
    </div>
{% endblock %}
